// @databehave/server: development placeholder for the admin UI script bundle.
// The real bundle is produced by `pnpm run build:admin` and written to
// `dist/admin/ui.js`. This stub exists so unit tests that exercise the
// `${path}/ui.js` route can read a file from `src/admin/` (where the
// runtime resolves it during tests). Do not import this file.
export {}
