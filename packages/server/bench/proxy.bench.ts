import { bench, describe } from 'vitest'

/**
 * Placeholder for the validation-proxy benchmark.
 *
 * Activates once Phase 2 item #7 (validation-proxy mode) lands. Until
 * then, this file exists so the `bench/` directory layout is stable
 * and consumers reading the plan can see the intended shape.
 */

describe('proxy validation overhead', () => {
  // TODO(phase-2 item #7): activate when validation-proxy lands.
  bench.skip('proxy validation overhead — pending phase-2 #7', () => {
    /* placeholder */
  })
})
