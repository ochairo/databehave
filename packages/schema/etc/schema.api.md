## API Report File for "@databehave/schema"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

// @public (undocumented)
export const arr: <S extends Schema>(item: S) => ArraySchema<S>;

// @public (undocumented)
export class ArraySchema<S extends Schema> extends Schema<Infer<S>[]> {
    constructor(item: S, opts?: {
        length?: number;
        minLength?: number;
        maxLength?: number;
    });
    // (undocumented)
    readonly _item: S;
    // (undocumented)
    length(n: number): ArraySchema<S>;
    // (undocumented)
    max(n: number): ArraySchema<S>;
    // (undocumented)
    min(n: number): ArraySchema<S>;
    // Warning: (ae-forgotten-export) The symbol "ArrayKind" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly _node: ArrayKind & {
        mods?: Modifiers;
    };
}

// @public
export type Axes = {
    readonly distribution?: Distribution;
    readonly occasionally?: readonly OccasionalOverride[];
    readonly eventually?: readonly EventuallyOverride[];
    readonly derived?: DerivedFn;
    readonly invariants?: readonly InvariantFn[];
    readonly domain?: DomainConstraint;
};

// @public (undocumented)
export const bool: () => BooleanSchema;

// @public (undocumented)
export class BooleanSchema extends Schema<boolean> {
    // (undocumented)
    readonly [Discrete]: true;
}

// @public
export class ConformError extends DataBehaveError {
    constructor(issues: readonly Issue[]);
    // (undocumented)
    readonly hint?: string;
    // (undocumented)
    readonly issues: readonly Issue[];
    readonly path: readonly (string | number)[];
}

// @public (undocumented)
export const createTrace: () => TraceCollector;

// @public
export abstract class DataBehaveError extends Error {
    abstract readonly hint?: string;
    abstract readonly path: readonly (string | number)[];
}

// @public (undocumented)
export type DatasetOptions<S extends Schema> = {
    readonly name: string;
    readonly schema: S;
    readonly identity: readonly string[];
    readonly n: number;
    readonly invariants?: readonly ((rows: readonly Infer<S>[]) => boolean)[];
    readonly input?: Readonly<Record<string, unknown>>;
    readonly seedPrefix?: string;
};

// @public
export const decimal: (precision: number, scale: number) => DecimalSchema;

// @public (undocumented)
export class DecimalSchema extends Schema<string> {
    // (undocumented)
    readonly [Continuous]: true;
    // (undocumented)
    readonly [Numeric]: true;
    max(value: string | number): DecimalSchema;
    min(value: string | number): DecimalSchema;
    // Warning: (ae-forgotten-export) The symbol "DecimalKind" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly _node: DecimalKind & {
        mods?: Modifiers;
    };
}

// @public
export type DerivedFn<T = unknown> = (ctx: GenContext) => T;

// @public
export const discriminated: <K extends string, M extends Readonly<Record<string, Schema<unknown>>>>(key: K, map: M) => Schema<{ [P in keyof M]: Infer<M[P]>; }[keyof M]>;

// @public (undocumented)
export type Distribution = WeightedDistribution | NormalDistribution | TypicalDistribution;

// @public
export type DomainConstraint = {
    readonly kind: 'values';
    readonly values: readonly unknown[];
} | {
    readonly kind: 'lookup';
    readonly fromField: string;
    readonly map: Readonly<Record<string, readonly unknown[]>>;
};

// @public (undocumented)
export const enum_: <V extends string | number>(values: readonly V[]) => EnumSchema<V>;

// @public (undocumented)
export class EnumSchema<V extends string | number> extends Schema<V> {
    // (undocumented)
    readonly [Discrete]: true;
    constructor(values: readonly V[]);
    // Warning: (ae-forgotten-export) The symbol "EnumKind" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly _node: EnumKind & {
        mods?: Modifiers;
    };
    // (undocumented)
    readonly _values: readonly V[];
}

// @public
export type EventuallyOverride = {
    readonly value: unknown;
    readonly every: number;
    readonly offset?: number;
};

// @public
export const expectStable: <S extends Schema>(schema: S, options?: MockOptions) => Infer<S>;

// @public
export type GenContext = {
    readonly root: unknown;
    readonly parent: Readonly<Record<string, unknown>>;
    readonly index?: number;
    readonly input?: Readonly<Record<string, unknown>>;
    readonly seed: string;
};

// @public
export const identityFor: (datasetName: string, identityKeys: readonly string[], row: Record<string, unknown>) => string;

// @public
export type Infer<S> = S extends Schema<infer T> ? T : never;

// @public (undocumented)
export const int: () => IntSchema & {
    readonly [Numeric]: true;
};

// @public
export class IntSchema extends NumberSchema {
    // (undocumented)
    readonly [Discrete]: true;
}

// @public
export type InvariantFn = (value: unknown, ctx: GenContext) => boolean;

// @public (undocumented)
export type Issue = {
    readonly path: readonly (string | number)[];
    readonly code: IssueCode;
    readonly message: string;
    readonly expected?: string;
    readonly received?: unknown;
};

// @public
export type IssueCode = 'array.expected' | 'array.length-mismatch' | 'array.too-long' | 'array.too-short' | 'boolean.expected' | 'decimal.expected' | 'decimal.not-numeric' | 'decimal.scale-exceeded' | 'derived.callback-threw' | 'derived.mismatch' | 'discriminated.expected-object' | 'discriminated.missing-tag' | 'discriminated.unknown-tag' | 'domain.lookup-mismatch' | 'domain.not-in-values' | 'enum.not-member' | 'invariant.callback-threw' | 'invariant.failed' | 'literal.not-equal' | 'null.expected' | 'number.expected' | 'number.not-integer' | 'number.too-large' | 'number.too-small' | 'object.expected' | 'required' | 'string.expected' | 'string.pattern-mismatch' | 'string.too-long' | 'string.too-short' | 'tuple.expected-array' | 'tuple.length-mismatch' | 'unexpected-null' | 'union.no-match';

// @public (undocumented)
export const literal: <V extends string | number | boolean | null>(value: V) => LiteralSchema<V>;

// @public (undocumented)
export class LiteralSchema<V extends string | number | boolean | null> extends Schema<V> {
    // (undocumented)
    readonly [Discrete]: true;
    // Warning: (ae-forgotten-export) The symbol "LiteralKind" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly _node: LiteralKind & {
        mods?: Modifiers;
    };
}

// @public
export const mock: <S extends Schema>(schema: S, options?: MockOptions) => Infer<S>;

// @public
export const mockDataset: <S extends Schema>(opts: DatasetOptions<S>) => Infer<S>[];

// @public (undocumented)
export type MockOptions = {
    readonly seed?: string | number;
    readonly input?: Readonly<Record<string, unknown>>;
    readonly index?: number;
    readonly trace?: TraceCollector;
    readonly stableBy?: StableByFn;
    readonly modifierProbs?: ModifierProbs;
    readonly prng?: (seed: number) => Rng;
};

// @public
export type ModifierProbs = {
    readonly default?: number;
    readonly optional?: number;
    readonly nullable?: number;
};

// @public (undocumented)
export type Modifiers = {
    readonly nullable?: boolean;
    readonly optional?: boolean;
    readonly hasDefault?: boolean;
    readonly defaultValue?: unknown;
    readonly description?: string;
    readonly axes?: Axes;
};

// @public
export type NormalDistribution = {
    readonly kind: 'normal';
    readonly mean: number;
    readonly stddev: number;
};

// @public (undocumented)
export const null_: () => NullSchema;

// @public (undocumented)
export class NullSchema extends Schema<null> {
}

// @public (undocumented)
export const num: () => NumberSchema & {
    readonly [Numeric]: true;
    readonly [Continuous]: true;
};

// @public (undocumented)
export class NumberSchema extends Schema<number> {
    // (undocumented)
    protected cloneWithNode(node: NumberKind & {
        mods?: Modifiers;
    }): this;
    // (undocumented)
    max(n: number): this;
    // (undocumented)
    min(n: number): this;
    // Warning: (ae-forgotten-export) The symbol "NumberKind" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly _node: NumberKind & {
        mods?: Modifiers;
    };
}

// Warning: (ae-forgotten-export) The symbol "Fields" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export const obj: <F extends Fields>(fields: F) => ObjectSchema<F>;

// Warning: (ae-forgotten-export) The symbol "InferObject" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export class ObjectSchema<F extends Fields = Fields> extends Schema<{
    [K in keyof InferObject<F>]: InferObject<F>[K];
}> {
    constructor(fields: F);
    correlate(fn: (row: InferObject<F>) => boolean): this;
    // (undocumented)
    readonly _fields: F;
    // Warning: (ae-forgotten-export) The symbol "ObjectKind" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly _node: ObjectKind & {
        mods?: Modifiers;
    };
}

// @public
export type OccasionalOverride = {
    readonly value: unknown;
    readonly p: number;
};

// @public (undocumented)
export const parse: <S extends Schema>(schema: S, value: unknown) => Infer<S>;

// @public (undocumented)
export const relate: <R extends Record<string, unknown>, K extends Extract<keyof R, string>>(rows: readonly R[], field: K, opts?: RelateOptions) => DerivedFn<R[K]>;

// @public (undocumented)
export type RelateOptions = {
    readonly pickBy?: 'random' | 'index' | ((ctx: GenContext) => number);
};

// @public
export type Replay<S extends Schema> = {
    (): Infer<S>;
    readonly options: Readonly<MockOptions>;
};

// @public
export const replay: <S extends Schema>(schema: S, options?: MockOptions) => Replay<S>;

// @public
export type Rng = {
    next(): number;
    int(min: number, max: number): number;
    pick<T>(items: readonly T[]): T;
};

// @public (undocumented)
export const safeParse: <S extends Schema>(schema: S, value: unknown) => SafeParseResult<Infer<S>>;

// @public (undocumented)
export type SafeParseResult<T> = {
    readonly ok: true;
    readonly value: T;
} | {
    readonly ok: false;
    readonly error: ConformError;
};

// @public
export class Schema<T = unknown> {
    // (undocumented)
    readonly [Continuous]: boolean;
    // (undocumented)
    readonly [Discrete]: boolean;
    // (undocumented)
    readonly [Numeric]: boolean;
    constructor(node: SchemaNode);
    _applyAxes(axes: Axes): this;
    // (undocumented)
    default(value: T): Schema<T>;
    derivedFrom<U extends T>(fn: DerivedFn<U>): this;
    // (undocumented)
    describe(text: string): this;
    eventually(every: number, value: T, opts?: {
        readonly offset?: number;
    }): this;
    in(constraint: DomainConstraint | readonly unknown[]): this;
    invariant(fn: InvariantFn): this;
    // (undocumented)
    readonly _node: SchemaNode;
    normal(this: this & {
        readonly [Continuous]: true;
    }, mean: number, stddev: number): this;
    // (undocumented)
    nullable(): Schema<T | null>;
    occasionally(value: T, p: number): this;
    // (undocumented)
    optional(): Schema<T | undefined>;
    // (undocumented)
    readonly _type: T;
    typically(this: this & {
        readonly [Numeric]: true;
    }, from: number, to: number): this;
    weighted(this: this & {
        readonly [Discrete]: true;
    }, weights: ReadonlyArray<readonly [T & (string | number | boolean), number]>): this;
    // (undocumented)
    protected withAxes(axes: Axes): this;
    // (undocumented)
    protected withModsPreserveType(patch: Partial<Modifiers>): this;
}

// @public
export class SchemaConflictError extends DataBehaveError {
    constructor(message: string, path?: readonly (string | number)[], hint?: string, code?: string);
    readonly code?: string;
    // (undocumented)
    readonly hint?: string;
    // (undocumented)
    readonly path: readonly (string | number)[];
}

// Warning: (ae-forgotten-export) The symbol "SchemaCore" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export type SchemaNode = SchemaCore & {
    readonly mods?: Modifiers;
};

// @public
export type StableByFn = (ctx: GenContext) => string | null | undefined;

// @public (undocumented)
export const str: () => StringSchema;

// @public (undocumented)
export class StringSchema extends Schema<string> {
    // (undocumented)
    readonly [Discrete]: true;
    // (undocumented)
    max(n: number): StringSchema;
    // (undocumented)
    min(n: number): StringSchema;
    // Warning: (ae-forgotten-export) The symbol "StringKind" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly _node: StringKind & {
        mods?: Modifiers;
    };
    // (undocumented)
    pattern(re: RegExp | string): StringSchema;
}

// @public
export type TraceAxis = 'default' | 'optional-skip' | 'nullable-null' | 'derived' | 'occasionally' | 'eventually' | 'invariant-pass' | 'invariant-fail' | 'domain' | 'distribution' | 'type';

// @public (undocumented)
export type TraceCollector = {
    readonly emit: (entry: TraceEntry) => void;
    readonly entries: readonly TraceEntry[];
    readonly axisFiredAt: (axis: TraceAxis) => readonly (readonly (string | number)[])[];
    readonly format: () => string;
};

// @public (undocumented)
export type TraceEntry = {
    readonly path: readonly (string | number)[];
    readonly axis: TraceAxis;
    readonly attempts?: number;
    readonly note?: string;
};

// @public (undocumented)
export const tuple: <T extends readonly Schema[]>(...items: T) => TupleSchema<T>;

// Warning: (ae-forgotten-export) The symbol "InferTuple" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export class TupleSchema<T extends readonly Schema[]> extends Schema<InferTuple<T>> {
    constructor(items: T);
    // (undocumented)
    readonly _items: T;
    // Warning: (ae-forgotten-export) The symbol "TupleKind" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly _node: TupleKind & {
        mods?: Modifiers;
    };
}

// @public
export type TypicalDistribution = {
    readonly kind: 'typical';
    readonly from: number;
    readonly to: number;
};

// @public (undocumented)
export const union: <T extends readonly Schema[]>(...options: T) => UnionSchema<T>;

// Warning: (ae-forgotten-export) The symbol "InferUnion" needs to be exported by the entry point index.d.ts
//
// @public (undocumented)
export class UnionSchema<T extends readonly Schema[]> extends Schema<InferUnion<T>> {
    constructor(options: T);
    // Warning: (ae-forgotten-export) The symbol "UnionKind" needs to be exported by the entry point index.d.ts
    //
    // (undocumented)
    readonly _node: UnionKind & {
        mods?: Modifiers;
    };
    // (undocumented)
    readonly _options: T;
}

// @public
export type WeightedDistribution = {
    readonly kind: 'weighted';
    readonly weights: ReadonlyArray<readonly [string | number | boolean, number]>;
};

// (No @packageDocumentation comment for this package)

```
